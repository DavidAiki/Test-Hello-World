version: '3.8'

services:
  hello-world:
    build: .
    ports:
      - "80:80"
      - "443:443"
    volumes:
      # SSL-Zertifikate mounten (self-signed für lokal, Let's Encrypt für Produktion)
      - ./certs:/etc/nginx/certs:ro
    restart: unless-stopped
